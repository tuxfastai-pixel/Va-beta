export function generateEnterpriseProposal(company: string) {
  return `
We help ${company} automate execution using AI agents.

We can:
- reduce development costs
- increase output speed
- automate repetitive workflows

We propose a pilot project to demonstrate value.

Let\'s schedule a call.
`;
}
