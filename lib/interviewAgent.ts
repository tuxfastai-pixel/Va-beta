export function generateInterviewQuestions(role: string) {
  return [
    `Why do you want to work as a ${role}?`,
    `How do you manage multiple tasks?`,
    `Describe your experience with remote work tools.`
  ]
}
